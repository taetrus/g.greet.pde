# UI-only configuration for com.kk.greet.ui (window geometry etc.).
# Texts do NOT belong here — they live in ../lang/messages[_xx].properties.
#
# window.width/window.height in pixels; 0 (or a missing key) means "pack to
# the preferred size of the content".
window.width=480
window.height=260
window.resizable=true
