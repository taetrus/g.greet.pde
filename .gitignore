# Compiled output (regenerated by Eclipse)
bin/
*.class

# Build / obfuscation outputs (regenerated by scripts + Maven)
Deployment/build/
obfuscation/target/

# macOS
.DS_Store

# Eclipse / PDE local state
.metadata/
.recommenders/
*.launch
# but keep the curated, shared launch config (referenced by scripts + docs)
!Deployment/launch/greet.launch
tmp/

# OS / editor
*~
*.swp
